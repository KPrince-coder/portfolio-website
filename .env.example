# ============================================================================
# Supabase Configuration
# ============================================================================
# Get these from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api

VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-public-key"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# ============================================================================
# Email Configuration (Client-Side)
# ============================================================================
# These are used by the client application

# Admin email to receive contact form notifications
VITE_ADMIN_EMAIL="contact@codeprince.qzz.io"

# Company information for email templates
VITE_COMPANY_NAME="CodePrince"
VITE_COMPANY_EMAIL="contact@codeprince.qzz.io"

# ============================================================================
# EmailJS Configuration (Alternative Email Service)
# ============================================================================
# Get these from https://dashboard.emailjs.com/admin
# EmailJS is a client-side email service (alternative to Resend)

# Public Key (safe to expose in frontend)
VITE_EMAILJS_PUBLIC_KEY="your_emailjs_public_key"

# Service ID (Gmail/Outlook/etc)
VITE_EMAILJS_SERVICE_ID="your_service_id"

# Template IDs for different email types
VITE_EMAILJS_TEMPLATE_NOTIFICATION="template_notification_id"  # Admin notifications
VITE_EMAILJS_TEMPLATE_AUTO_REPLY="template_autoreply_id"       # Automatic reply to sender
VITE_EMAILJS_TEMPLATE_MANUAL_REPLY="template_manual_reply_id"  # Manual reply from admin panel

# ============================================================================
# Secondary EmailJS Account (Optional - Free Tier Workaround)
# ============================================================================
# Use a second EmailJS account to bypass the 2-template limit on free tier
# Account 1: Notification + Auto-reply (2 templates)
# Account 2: Manual reply (1 template)
# Total: 3 templates across 2 free accounts!

# VITE_EMAILJS_SECONDARY_PUBLIC_KEY="your_secondary_public_key"
# VITE_EMAILJS_SECONDARY_SERVICE_ID="service_secondary_id"

# Site Configuration
VITE_SITE_NAME="CodePrince"
VITE_SITE_URL="https://codeprince.qzz.io"
VITE_DEFAULT_AUTHOR="Prince Kyeremeh"
VITE_TWITTER_HANDLE="@codeprince"

# ============================================================================
# Email Configuration (Server-Side - Supabase Edge Functions)
# ============================================================================
# These are NOT set in .env file, but via Supabase secrets:
#
# Set via Supabase CLI:
#   supabase secrets set RESEND_API_KEY=re_your_api_key_here
#   supabase secrets set FROM_EMAIL=noreply@yourdomain.com
#   supabase secrets set ADMIN_URL=https://yourdomain.com/admin
#   supabase secrets set ENVIRONMENT=production
#
# Or via Supabase Dashboard:
#   Project Settings > Edge Functions > Secrets
#
# Required secrets:
# - RESEND_API_KEY: Your Resend API key (get from https://resend.com/api-keys)
# - FROM_EMAIL: Email address to send from (must be verified in Resend)
# - ADMIN_URL: URL to your admin panel (for notification links)
# - ENVIRONMENT: production, staging, or development

# ============================================================================
# Development Notes
# ============================================================================
# 1. Copy this file to .env and fill in your values
# 2. Never commit .env to version control (it's in .gitignore)
# 3. For production, set environment variables in your hosting platform
# 4. For Supabase Edge Functions, use `supabase secrets set` command
